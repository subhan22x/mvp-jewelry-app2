CREATE TABLE "QuoteRequest" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "requestId" TEXT,
    "resultId" TEXT,
    "videoId" TEXT,
    "designedImageUrl" TEXT,
    "videoUrl" TEXT,
    "generatedAt" DATETIME,
    "productType" TEXT,
    "styleId" TEXT,
    "text" TEXT,
    "twoTone" BOOLEAN,
    "primaryMetal" TEXT,
    "secondaryMetal" TEXT,
    "emblem" TEXT,
    "diamondQuality" TEXT,
    "customerName" TEXT NOT NULL,
    "customerPhone" TEXT NOT NULL,
    "customerEmail" TEXT NOT NULL,
    "status" TEXT NOT NULL DEFAULT 'pending',
    "quotedPriceCents" INTEGER,
    "quoteNotes" TEXT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "QuoteRequest_requestId_fkey" FOREIGN KEY ("requestId") REFERENCES "Request" ("id") ON DELETE SET NULL ON UPDATE CASCADE,
    CONSTRAINT "QuoteRequest_resultId_fkey" FOREIGN KEY ("resultId") REFERENCES "Result" ("id") ON DELETE SET NULL ON UPDATE CASCADE,
    CONSTRAINT "QuoteRequest_videoId_fkey" FOREIGN KEY ("videoId") REFERENCES "VideoGeneration" ("id") ON DELETE SET NULL ON UPDATE CASCADE
);
